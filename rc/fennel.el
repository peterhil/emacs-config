;; ==============================================================================
;; Fennel mode
;; ------------------------------------------------------------------------------

(use-package "fennel-mode"
  :ensure t
  :config
  (add-to-list 'auto-mode-alist '("\\.fnl\\'" . fennel-mode)))
